# Admin UI is packaged into the Spring Boot backend and should call the API on
# the same origin by default. Only set VITE_ADMIN_API_BASE_URL when deploying
# the frontend behind a separate reverse proxy/domain.
VITE_ADMIN_API_BASE_URL=https://green-go-246422-5-1422183264.sh.run.tcloudbase.com
VITE_ADMIN_ROUTER_BASE=
