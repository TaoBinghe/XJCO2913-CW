VITE_AMAP_KEY=YOUR_AMAP_KEY
# Leave empty for the normal same-origin deployment where admin-ui is served
# by the backend itself. Set a full https://... URL only if the admin frontend
# is deployed separately from the API service.
VITE_ADMIN_API_BASE_URL=
VITE_ADMIN_ROUTER_BASE=
